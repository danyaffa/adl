{
  "name": "adl-platform",
  "version": "1.0.0",
  "description": "ADL Universal Ad Tracking Platform",
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp index.html .vercel/output/static/ && cp -r public .vercel/output/static/"
  }
}
