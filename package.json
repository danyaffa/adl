{
  "name": "adl-tracker",
  "version": "1.0.0",
  "description": "ADL Tracker & Optimization Platform",
  "scripts": {
    "start": "npx serve public -s",
    "build": "echo 'Static build complete'"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}
